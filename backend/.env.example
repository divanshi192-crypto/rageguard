# Copy to .env and fill in secrets. Never commit .env.

GROQ_API_KEY=

# Your deployed Next.js URL(s), comma-separated, for CORS.
# FRONTEND_URL=https://your-app.vercel.app

# Optional: only if you use Claude instead of Groq
# ANTHROPIC_API_KEY=
